## Packages
date-fns | Formatting dates and times beautifully
framer-motion | Page transitions and scroll animations
react-hook-form | Form state management and validation
@hookform/resolvers | Zod validation resolver for forms
clsx | Conditional class names
tailwind-merge | Merging tailwind classes intelligently

## Notes
Requires Shadcn UI components which are already present in the project.
Expects the backend API to return matching types defined in @shared/routes.
Uses Replit Auth hook `useAuth()` at `client/src/hooks/use-auth.ts`.
Images: Includes one Unsplash hero image for the landing page.
